export function Button(props) {

    //const arrayDeBotones = []

    //bucle for que vaya rellenando el array con los botones

    /* return (
        <button id={miId}>
            {props.nombre}
        </button>
    ) */

    return (
        <button>
            😀
            {props.nombre}
        </button>
    )
}